-- ==========================================================
-- INSERT USER LOGIN
-- ==========================================================
-- Gunakan script ini untuk insert user login dengan password yang di-hash SHA2

-- Contoh dengan SHA2 hashing (recommended):
-- INSERT INTO user_login (username, password) VALUES ('dosen1', SHA2('password123', 256));

-- Catatan:
-- - Ganti 'dosen1' dengan username Anda
-- - Ganti 'password123' dengan password yang Anda inginkan
-- - SHA2('password', 256) akan otomatis hash password saat insert

-- Silakan tambahkan query INSERT Anda sendiri di bawah ini:
